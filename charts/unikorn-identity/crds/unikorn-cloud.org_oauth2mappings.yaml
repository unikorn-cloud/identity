---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.1
  name: oauth2mappings.unikorn-cloud.org
spec:
  group: unikorn-cloud.org
  names:
    categories:
    - unikorn
    kind: OAuth2Mapping
    listKind: OAuth2MappingList
    plural: oauth2mappings
    singular: oauth2mapping
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.domain
      name: domain
      type: string
    - jsonPath: .spec.providerName
      name: provider
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OAuth2Mapping describes an identity mapping.  The main job of
          this type is to take an email address identity, extract the domain and use
          it to resolve an identity provider.  It also is the place where users within
          that domain can be allowed based on groups/claims offered by that identity
          provider to limit access.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: OAuth2MappingSpec defines the required configuration for
              the server.
            properties:
              domain:
                description: Domain is used by unikorn-identity to map an end-user
                  provided email address to an identity provider.
                type: string
              providerName:
                description: ProviderName is the name of an oauth2/oidc provider.
                type: string
            required:
            - domain
            - providerName
            type: object
          status:
            description: OAuth2MappingStatus defines the status of the server.
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
